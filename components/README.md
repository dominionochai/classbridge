# Components

The requested upstream sources were license-checked via the GitHub API before this scaffold was committed. The GitHub Contents/Commit APIs available to this agent cannot perform a local recursive `git clone` of arbitrary third-party repositories or upload their complete trees in one operation. Therefore these directories contain attribution manifests rather than falsely claiming vendored source. Use the upstream URLs below to clone into these exact paths on a machine with Git, retaining each upstream license file.

Verified: 2026-09-19 UTC.
