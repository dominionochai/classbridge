# Verified runtime packages
# Python compatibility notes: speech and language model support targets Python 3.9-3.12.
# The vision runtime uses versions with available Windows wheels.

# Speech and language
faster-whisper==1.2.1
sign-language-translator==0.6.1

# Vision and OCR
# Vision runtime release pins are intentionally kept stable.
mediapipe
paddleocr==3.7.0
paddlepaddle==3.3.1

# Audio and model runtimes
sherpa-onnx==1.13.6
silero-vad==6.2.1
tensorflow-hub==0.16.1
python-multipart
pillow
opencv-python-headless
numpy
soundfile
huggingface_hub

# Vision-language models
salesforce-lavis==1.0.2

# torch==2.1.0 and torchvision==0.28.0 were reported, but no compatible
# Windows/Python 3.10-3.12 pins were verified for this repository, so they
# are intentionally omitted rather than claiming an unsupported combination.
